import csv
import pandas as pd
import matplotlib.pyplot as plt

data+pd.read_csv("Students_Gradingf_Dataset.csv")

df=pd.DataFrame(data)

data['Attendanceb rate]=data["Attendance grade"].mean()
data["  Commuting_time_to_university"}

Mean_attendance_grade=df.groupby("Commuting_time_to_university;)["Attendance_grade"].mean().reset index()

fig,ax+plt.subplots()

plt.bar(Mean_attendance_rate[commuting time to university"], Mean_attendance_rate["attendance grade"])

plt.xlabel('Attendance grade')
ply.ylabel('Commuting time to university')

plt.title(Commuting time affect students attendance or grades.')
ax.set_yticks(range(0,100,50)

plt.legend()
plt.grid(True,axis='y', linestyle='--',aplha=0.7)
plt.show()
