Import panadas as pd
import matplotlib.pyplot as plt

avg_quiz_by_dept= df.groupby('Department')[@quizzes_avg'].means().sort_values(ascending=False)

plt.figure(figsize=(10,6) )
avg_quiz_by_dept.plot(kind='bar',colour='skyblue')
plt.title('average quiz scores by department')
plt.xlabel(department')
plt.ylabel('Average quiz scores')
plt.axhline(df['Quizzes_avg').means(),. colours+'red', linestyle='--', label='overall average')
plt.xticks(rotation=45)
plt.tight_layoutr()
plt.show()
