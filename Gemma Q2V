import csv
import pandas as pd
import matplotlib.pyplot as plt

data = pd.read_csv("Students_Grading_Dataset.csv")

df = pd.DataFrame(data)

data['weekly work hours] = data['hours per week'].mean()
data['study hours per week'] = data['study hours per week’]

Mean Average_grade = df.groupby('study_hours_per_week
;)['Average_grade'].mean().reset_index()

fig, ax = plt.subplots()

plt.bar(Mean_average_grade ['study hours per week'], Mean_weekly work hours rate['Average grade'])

plt.xlabel('Average  grade')
plt.ylabel('study_hours_per_week')
plt.title('Part time work affect students grades and study habits.')
ax.set_yticks(range(0, 100, 20)

plt.legend()
plt.grid(True, axis= 'y', linestyle='--', alpha= 0.7)
plt.show()

